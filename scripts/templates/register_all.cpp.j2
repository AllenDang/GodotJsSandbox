// AUTO-GENERATED FILE - DO NOT EDIT
// Generated by generate_bindings.py

#include "generated_classes.gen.h"

namespace jsb {
namespace generated {

void register_all_classes(JSContext* ctx, JSValue global) {
    // Create the __godot_classes registry once
    JSValue classes = JS_NewObject(ctx);
    JS_SetPropertyStr(ctx, global, "__godot_classes", classes);
    // Note: global now owns a reference to classes
    // We keep our reference to pass to registration functions

{% for class_name in classes %}
    register_{{ class_name }}_bindings(ctx, global, classes);
{% endfor %}

    // Note: We don't free 'classes' because JS_SetPropertyStr does NOT add a reference
    // The global object now owns the only reference to classes
}

} // namespace generated
} // namespace jsb
