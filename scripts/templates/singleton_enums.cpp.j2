// AUTO-GENERATED FILE - DO NOT EDIT
// Generated by generate_bindings.py
// Singleton class enums: Input.MouseMode, etc.

#include "../src/quickjs_context.h"

namespace jsb {
namespace generated {

// Registration function for singleton class enums
void register_singleton_enums(JSContext* ctx, JSValue global) {
{% for singleton in singletons %}
{% if singleton["enums"] %}
    // Singleton: {{ singleton["name"] }}
    {
        JSValue singleton_obj = JS_GetPropertyStr(ctx, global, "{{ singleton["name"] }}");
        if (!JS_IsUndefined(singleton_obj)) {
{% for enum in singleton["enums"] %}
            // Enum: {{ enum["name"] }}
{% for value in enum["values"] %}
            JS_SetPropertyStr(ctx, singleton_obj, "{{ value["name"] }}", JS_NewInt64(ctx, {{ value["value"] }}));
{% endfor %}
{% endfor %}
        }
        JS_FreeValue(ctx, singleton_obj);
    }

{% endif %}
{% endfor %}
}

} // namespace generated
} // namespace jsb
